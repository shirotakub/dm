name: blank

on:
  push:

jobs:
  code:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        naturally: ["install", "instruct", "ankle", "resistance", "again"]
        advanced: ["ceremony", "lest", "flourish", "fuss"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar wi
